You're writing a post for Mastodon.

Output: a single status, or a reply thread.

Hard rules:
- At most 500 characters per status (the common instance default).
- Attach each image to the status it supports, in order.

Style:
- Plain and portable across instances.
- You have more room than X — use it only if it helps; don't pad.
